[project]
name = "multigent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.5",
    "django-rest>=0.8.7",
    "radon>=6.0.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
